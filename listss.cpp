#include <bits/stdc++.h>
using namespace std;

int main()
{
    // operations -->
    // push_back()
    // push_front()
    // pop_back()
    // pop_front()
    // begin(), end(), rbegin(), rend(), size(), empty(), at(), clear(), remove()

    list<int> ls;
    ls.push_front(1);
    ls.push_front(2);
    ls.push_front(3);
    ls.remove(3);
}